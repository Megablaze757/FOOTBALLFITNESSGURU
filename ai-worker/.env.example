# Copy to `.env` (gitignored). The service runs without ANTHROPIC_API_KEY using a
# deterministic fallback narrative — set it to enable the LLM coach.

ANTHROPIC_API_KEY=
# Shared secret the Edge Function sends as the `x-worker-key` header.
WORKER_API_KEY=
PORT=8000
